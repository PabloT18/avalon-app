name: authentication_repository
description: "Dart package which manages the authentication domain"
version: 0.0.1

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  cache:
    path: ../cache
  shared_models:
    path: ../shared_models
  equatable: ^2.0.5
  dio: ^5.4.3+1
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

publish_to: none
