import 'package:flutter/material.dart';

class CrearCasoPage extends StatelessWidget {
  const CrearCasoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('CrearCasoPage'),
        elevation: 6,
      ),
      body: const Center(
        child: Text('CrearCasoPage'),
      ),
    );
  }
}
